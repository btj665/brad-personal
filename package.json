{
  "name": "news-reader",
  "version": "1.0.0",
  "description": "Browser-based news reader with clustered headlines and an AI question panel",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "dist": "node scripts/make-dist.mjs",
    "dist:exe": "node scripts/make-dist.mjs --exe"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^6.21.0",
    "esbuild": "^0.28.1"
  }
}
